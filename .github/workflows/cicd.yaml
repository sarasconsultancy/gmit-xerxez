name: Medical_Expenses

on: 
    push:
        branches: ["main"]
    pull_request:
        branches: ["main"]

permissions:
    contents: read

jobs:
    build:
        runs-on: unbuntu-latest

        steps:
            - uses: actions/checkout@v3
            - name: Set Up Python 3.11
              uses: actions/checkout@v3
              with:
                python-version: "3.11"
